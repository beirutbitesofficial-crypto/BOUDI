'use strict';

// Hostinger loads this entry with require(), so startup must be unconditional.
// server.js prepares persistent storage before it opens the database.
module.exports = require('../server.js');
